language: go
go:
  - 1.9.x
  - master
before_install:
  - curl https://glide.sh/get | sh
script:
  - glide i
  - rm -rf ./vendor/github.com/hashicorp/serf/vendor
  - mkdir dist
  - go build -i -o ./dist/djob .